# one-way-ssl

This APi contains the code for one way ssl and also explais how we use DB connetor in mule

- use this url (http://localhost:8081/api/clientapp) to call the client API (i.e my-db-pocFlow). which will all he server (i.e db-main-flow-server)
